# The script is UTF-8 WITH BOM on purpose (Windows PowerShell 5.1 reads a BOM-less .ps1
# as ANSI). Git keeps a BOM as ordinary content; what it must NOT do is rewrite bytes.
*.ps1 -text
# cmd.exe is happiest with CRLF in a batch file
*.bat text eol=crlf
*.md  text eol=lf
*.txt text eol=lf
